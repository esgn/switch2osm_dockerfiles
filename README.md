# switch2osm_dockerfiles

Dockerfiles for building an osm tile server

## Debian Jessie

- docker build the dockerfile
- launch and execute startup.sh in the /home/osm folder (sudo docker -it for accessing vm prompt)
- access map viewer at http://your_vm_IP_adress/
